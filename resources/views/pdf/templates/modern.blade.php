@extends('pdf.expose')
{{-- Modern template inherits from base but could override sections if we had them. --}}
{{-- Since the base is already quite modern, I will just ensure the directory exists and the base handles it. --}}
